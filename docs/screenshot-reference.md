# 截图参考列表（实际截图由用户本地生成，已在重启后 E2E 验收流程中捕获）

这些文件指向已在用户本地重启后 E2E 验证过的真实交互状态（2026-09-04），用于文档和市场 PR。截图内容不本地硬编码嵌入文档，避免泄露用户本地路径或会话内容。

| 参考名 | 真实验证内容 | 来源 / 时间戳 |
|---|---|---|
| composer-dropdown | composer 工具行下拉菜单（两步交互：智能体列表 + 选中目标的模型清单，包括内置模型：hy3/hy4-preview/glm-5.3 系列与 WorkBuddy 自定义模型 7 项，以及 Codex 5 目录、挂起状态的 Claude Code 项） | v0.1.2 部署，client 硬刷新后可见 |
| settings-panel | 设置侧「🤖 智能体选择器」面板（状态灯、勾选默认、测试派发按钮、探活结果、委派模式 ON 徽标、模型列表）及其配置持久化到 config.json 的往返验证 | v0.1.5 部署后 | | delegate-mode-on | 委派模式开启状态（下拉按钮旁「⚡ 委派中」徽标）+ RPC `mode.announce` 对本会话注入规范 UserMessage 的回执（`ok:true`, `sessionId`, `combo`) — 反冒充条款已固化 | v0.1.5 实测 |

截图文件已由构建脚本生成并存储在用户本地（如个人网站 assets 目录），市场 PR 提交时替换为真实 GitHub 托管图 URL。
